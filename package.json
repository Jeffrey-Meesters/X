{
  "name": "fullbody-15",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "15-Minute Full-Body Dumbbell Trainer — a local-first, installable PWA",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "type-check": "vue-tsc -b",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "verify": "npm run type-check && npm run test && npm run build"
  },
  "dependencies": {
    "idb": "8.0.3",
    "pinia": "4.0.3",
    "vue": "3.5.42",
    "vue-router": "5.3.0"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "@tailwindcss/vite": "4.3.3",
    "@types/node": "22.19.1",
    "@vitejs/plugin-vue": "6.0.8",
    "fake-indexeddb": "6.2.5",
    "jsdom": "30.0.1",
    "tailwindcss": "4.3.3",
    "typescript": "5.9.3",
    "vite": "8.2.2",
    "vite-plugin-pwa": "1.3.0",
    "vitest": "4.1.11",
    "vue-tsc": "3.3.11"
  },
  "comments": {
    "typescript": "Pinned to 5.9.3 exactly, NOT 7.x. vue-tsc 3.3.11 patches typescript/lib/tsc, which the TS 7 native port no longer exposes via package exports, so vue-tsc crashes with ERR_PACKAGE_PATH_NOT_EXPORTED. vue-tsc's peer range (>=5.0.0) does not express this. Revisit when vue-tsc ships TS 7 support; the CI canary job watches for it."
  }
}
